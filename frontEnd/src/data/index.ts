export const sportNames = require('./sports.json');
export const seasonNames = require('./seasons.json');
export const countryNames = require('./countrys.json');

export const years = Array.from(Array(90), (_, index) => (index + 1932).toString());

export const players = [];